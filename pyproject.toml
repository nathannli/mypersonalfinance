[project]
name = "mypersonalfinance"
version = "0.1.0"
description = "Personal finance transaction loaders for PostgreSQL."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastexcel",
    "fastmcp>=3.2.4,<4",
    "macos-messages",
    "numpy",
    "polars",
    "psycopg",
    "psycopg-binary",
    "python-dotenv",
    "requests",
    "sqlalchemy",
]

[project.optional-dependencies]
wealthsimple = [
    "wealthsimpleton",
]

[tool.uv.sources]
wealthsimpleton = { path = "../Wealthsimpleton" }

[dependency-groups]
dev = [
    "pre-commit",
    "ruff",
]

[tool.uv]
package = false
